//JS Literal Notation
// var thing = { key: value, key: value };
// a way to create an object
function Calculator() {
  this.add = function(a, b) {
    return a + b;
  };
  this.subtract = function(a, b) {
    return a - b;
  };
}
